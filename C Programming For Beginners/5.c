#include<stdio.h>
int main(){
    
    int age;
    double height;
    printf("Enter the age: %d\n",age); //Gets the age of the person
    scanf("%d",&age);
    printf("Enter the height:%lf\n",height);//Gets the height of the person
    scanf("%lf",&height);
    printf("The age is:%d\n",age);//Prints the age of the person
    printf("The height is :%lf\n",height);//Prints the height of the person
    return 0;
}